{
  "packages": ["packages/*"],
  "npmClient": "yarn",
  "useWorkspaces": true,
  "version": "independent",
  "command": {
    "publish": {
      "allowBranch": "main",
      "conventionalCommits": true,
      "createRelease": "github",
      "ignoreChanges": [
        "**/__fixtures__/**",
        "**/*.snap",
        "**/*.test.*",
        "**/*.stories.*",
        "**/*.md"
      ],
      "message": "chore(release): publish"
    }
  }
}
